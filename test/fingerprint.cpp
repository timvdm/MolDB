#include <iostream>
#include "../src/fingerprint.h"

#include "test.h"


using namespace MolDB;

int main()
{
  /*
  unsigned long word1 = 5;
  Fingerprint fp1(64, &word1);
  MOLDB_COMPARE(fp1.count(), 2);
  
  unsigned long word2 = 29;
  Fingerprint fp2(64, &word2);
  MOLDB_COMPARE(fp2.count(), 4);

  MOLDB_COMPARE((fp1 | fp2).count(), 4);
  MOLDB_COMPARE((fp1 & fp2).count(), 2);
 `*/


}
